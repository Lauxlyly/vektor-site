{
  "name": "vektor-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node test/security.test.js && node test/extract.test.js && node test/contrast.test.js && node test/page-guardrails.test.js",
    "test:e2e": "playwright test",
    "check:publish": "node scripts/check-publishable.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.53.0",
    "@upstash/ratelimit": "^2.0.5",
    "@upstash/redis": "^1.34.3",
    "resend": "^4.0.0",
    "stripe": "^16.0.0",
    "youtube-transcript": "^1.2.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
